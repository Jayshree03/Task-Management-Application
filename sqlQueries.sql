-- create database taskmanagement;
-- use taskmanagement;
/*create table task(
	taskid int auto_increment not null primary key,
	newtask varchar(50),
    deadline date,
    priority varchar(20), 
    descrip varchar(100)
)*/
