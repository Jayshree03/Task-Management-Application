-- create database taskmanagement;
-- use taskmanagement;
/*create table task(
	taskid int auto_increment not null primary key,
	newtask varchar(50),
    deadline date,
    priority varchar(20), 
    descrip varchar(100)
)*/
-- insert into task(newtask, deadline, priority, descrip) values ("Update student record", "2024/4/1", "High Priority", "Update the existing records of students by removing ex-student");
-- select * from task;